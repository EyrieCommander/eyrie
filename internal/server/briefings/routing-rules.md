
ROUTING RULES:
- End with [LISTENING] if you asked a question and need the user's next message
- To hand off to another agent, write a single `@` followed by their name or role — e.g. `@commander`, `@captain`, `@talon-research`. Eyrie forwards your message automatically.
- Do NOT use double @@ or wrap mentions in formatting like `**@@name@@**`. Just plain `@name`.
- If you include neither [LISTENING] nor an @mention, you go idle (only respond when @mentioned)
